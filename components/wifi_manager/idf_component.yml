dependencies:
  idf:
    version: ">=6.0.1"
  espressif/esp_wifi_remote:
    version: "1.3.0"
    public: true
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_hosted:
    version: "==2.9.3"
    public: true
    rules:
      - if: "target in [esp32p4]"

targets:
  - esp32p4
