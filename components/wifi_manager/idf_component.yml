dependencies:
  idf:
    version: ">=6.0.2"
  espressif/esp_wifi_remote:
    version: ">=1.3.1"
    public: true
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_hosted:
    version: "==2.12.9"
    public: true
    rules:
      - if: "target in [esp32p4]"

targets:
  - esp32p4
